import type { JsonPermissionsRequest } from './types';
import { InterfaceMethod } from '../../types';

export const processPermissionsRequest: InterfaceMethod = async (
  _message: JsonPermissionsRequest, _messageStore, _didResolver): Promise<void> => {

  return;
};